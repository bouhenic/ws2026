# Secrets — ne jamais committer
.env
.env.*
!.env.example

# Ancien code (contient des tokens en dur)
legacy/

node_modules/
npm-debug.log*
.DS_Store

# Build du dashboard (généré par Vite)
dashboard/dist/
